import SpaceAuthPage from './SpaceAuthPage'

// Note: Signup functionality has been removed as it's not allowed in this system
// Users authenticate directly with their credentials for PS selection
export default function AuthPage() {
  return <SpaceAuthPage />
}